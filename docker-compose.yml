services:
  gnest-website:
    build:
      context: .
      dockerfile: Dockerfile
    image: gnest-website:local
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped
